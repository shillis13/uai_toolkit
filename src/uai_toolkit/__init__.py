"""uai_toolkit — portable AI tooling."""

# THE single source of truth for the package version. pyproject.toml reads this
# attribute (see [tool.setuptools.dynamic]), so bump it HERE only — the packaged
# metadata, `uai-toolkit --version`, and the git tag then all agree.
# Versioning: semantic (MAJOR.MINOR.PATCH); releases are git tags named vX.Y.Z.
__version__ = "0.2.0"
